{
  "name": "@grenz/haven",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync": "cd ../../packages/grenz-webmcp && bun run build && cp dist/grenz-install.js ../../apps/haven/public/grenz-install.js",
    "dev": "bun run sync && vite",
    "build": "bun run sync && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "grenz-webmcp": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
